---
- name: CONFIG VRF ON PE ROUTERS 
  hosts: R1,R2,R3,R7,R9
  gather_facts: false

  tasks:
    - name: VRF FOR BGP CONFIG
      cisco.ios.ios_config:
        lines: "{{ lookup('template', 'templates/vrf.j2') }}"